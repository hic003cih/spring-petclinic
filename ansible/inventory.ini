# Ansible inventory: the production web server (an OrbStack Linux VM).
[production]
petclinic-prod.orb.local

[production:vars]
ansible_user=leif
ansible_ssh_private_key_file=/var/jenkins_home/.ssh/id_ed25519
ansible_python_interpreter=/usr/bin/python3
